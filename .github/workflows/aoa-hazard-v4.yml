name: AOA Hazard V4

on:
  push:
    branches: [research-aoa-hazard-v4]
    paths:
      - "research/aoa_hazard_v4/**"
      - ".github/workflows/aoa-hazard-v4.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements-backtest.txt scikit-learn
      - run: python -m py_compile research/aoa_hazard_v4/hazard_v4.py
      - run: python research/aoa_hazard_v4/hazard_v4.py
      - uses: actions/upload-artifact@v4
        with:
          name: aoa-hazard-v4-results
          path: research/aoa_hazard_v4/output/
          if-no-files-found: error
          retention-days: 30
