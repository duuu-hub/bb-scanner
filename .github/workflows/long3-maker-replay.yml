name: LONG3 Maker Replay
on:
  push:
    branches: [main]
    paths:
      - 'analysis_maker_replay.py'
      - '.github/workflows/long3-maker-replay.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  replay:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests
      - run: python analysis_maker_replay.py
