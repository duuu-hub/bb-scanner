name: Execution Safety Engine Tests

on:
  push:
    branches:
      - demo-trading-env
    paths:
      - 'execution_safety.py'
      - 'config/trading_mode.json'
      - 'tests/test_execution_safety.py'
      - '.github/workflows/execution-safety-tests.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  safety-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run safety tests
        run: python -m unittest tests/test_execution_safety.py -v
