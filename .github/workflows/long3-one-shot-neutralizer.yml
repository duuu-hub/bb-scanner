name: LONG3 One-Shot Queue Neutralizer

on:
  push:
    paths:
      - '.github/workflows/long3-one-shot-neutralizer.yml'

concurrency:
  group: bitget-bb-scanner-long3
  cancel-in-progress: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No trading. Queue neutralizer only."
