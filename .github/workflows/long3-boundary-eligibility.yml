name: LONG3 Boundary Eligibility Replay
on:
  push:
    branches: [main]
    paths:
      - 'analysis_boundary_eligibility.py'
      - '.github/workflows/long3-boundary-eligibility.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  replay:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests
      - run: python analysis_boundary_eligibility.py
